import { CustomExpandable } from ".";

export default {
  title: "Components/CustomExpandable",
  component: CustomExpandable,
};

export const Default = {
  args: {
    isOpen: true,
    className: {},
    text: "List item",
  },
};
